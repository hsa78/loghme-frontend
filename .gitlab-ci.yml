docker-build-master:
 # Official docker image.
 image: docker:19-git
 stage: build
 #services:
 #  - docker:dind
 before_script:
   - docker login -u "falagheband" -p "ftmal3922" registry.hub.docker.com/repository/docker/falagheband/ie_frontend
 script:
   - docker build -t myapp .
   - docker push falagheband/ie_frontend
 only:
   - master
